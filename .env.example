# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Google Sheets API Configuration
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=path/to/your/service-account-key.json

# Instructions for Google Sheets Setup:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select an existing one
# 3. Enable the Google Sheets API
# 4. Create a service account and download the JSON key file
# 5. Share your Google Sheet with the service account email
# 6. Copy the spreadsheet ID from the URL and paste it above
# 7. Set the path to your service account key file